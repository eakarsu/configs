{
  "name": "configuration-reference-boundary",
  "version": "1.0.0",
  "private": true,
  "description": "Non-executable integrity boundary for the retained configuration archive",
  "type": "module",
  "scripts": {
    "start": "./start.sh",
    "test": "node --test test/*.test.mjs",
    "check": "node bin/validate-reference.mjs && node --test test/*.test.mjs"
  }
}
