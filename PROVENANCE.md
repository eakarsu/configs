# Provenance and responsibility record

The configured origin is `https://github.com/eakarsu/configs`. The reachable history contains one commit, `025569cdad9fd7f9698699a2e21c18f88bd81b59`, authored by Erol Akarsu on 2026-04-24. The snapshot contains 1,232 root JSON configurations; all parse and share the root keys `appName`, `dbName`, `folderName`, and `features`. The content inventory digest and algorithm are pinned in `REFERENCE_BOUNDARY.json`.

No `LICENSE`, `COPYING`, or `NOTICE` file exists in the tree or reachable commit. Repository ownership does not imply a public license grant. Until the copyright owner selects a license, the archive is internal-only and must not be redistributed. A named product owner and a named security-patching owner are also absent; those assignments are external governance decisions and are deliberately not invented here.

Configurations contain generated prompts and sample records. Duplicate application names and generated folder targets occur across variants, so filenames are identities and records must not be merged by `appName` or `folderName`. Nothing here proves third-party data rights, regulatory suitability, provider contracts, or correctness. Updates must be reviewed snapshots with a regenerated digest, schema/security checks, provenance evidence, and a documented reason; automated upstream ingestion is unsupported.
